fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
pydantic==2.9.2
pydantic-settings==2.5.2
alembic==1.13.3
apscheduler==3.10.4
slowapi==0.1.9
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
spacy==3.7.5
weasyprint==60.2
pydyf==0.10.0
jinja2==3.1.4
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
email-validator==2.2.0
python-multipart==0.0.12
psycopg2-binary==2.9.9
redis==5.0.8
python-dotenv==1.0.1
httpx==0.27.2
sentry-sdk[fastapi]==2.18.0
pyyaml==6.0.2
# scipy is used by the drift detector (Mann-Whitney U). Wheel is ~35 MB and
# the installed footprint is ~100 MB, sharing numpy with spacy/presidio.
# Acceptable inside the main image; drift detection has no LLM deps and
# stays in the main app rather than the eval worker.
scipy==1.14.1

# Eval-execution deps (ragas / datasets / langchain-*) live in
# requirements-evals.txt. The main API image deliberately does NOT install
# them so it fits inside Render's 512 MB free tier. The eval framework's
# data model, API routes, and dashboard still ship with the main app; only
# the heavy execution lives in a separate worker. See services/evaluators/
# for the runtime guard that surfaces a clean install hint if execution is
# attempted without those deps present.

pytest==8.3.3
