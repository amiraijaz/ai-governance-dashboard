def _calculate_cost(model: str, prompt_tokens: int, completion_tokens: int) -> float:
    """Internal placeholder. Cost is computed server-side from the
    model_pricing table — this stub exists only to satisfy historical
    imports and is not part of the SDK's public API.
    """
    return 0.0
